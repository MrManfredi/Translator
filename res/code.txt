int mark, var
{       prettyPlace:
        in >> mark
        var = 10
        repeat mark = - (var / 2 - 3 * var) + mark
        until (mark > 60)
	out << mark
        if (mark <= 85) goto prettyPlace:
        mark = (var <= 20) ? 1 : 100
	out << mark
}
